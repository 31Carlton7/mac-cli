// KeynoteModule: AppleScript-backed Keynote store and subcommands.
// iWork open-document model — verbs address open documents by name;
// `new` creates (and optionally saves) one, `export` writes pdf/pptx.
