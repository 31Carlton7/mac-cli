// NumbersModule: AppleScript-backed Numbers store and subcommands.
// iWork open-document model — verbs address open documents by name;
// cell get/set via 1-based sheet/table + A1 refs, `export` writes pdf/xlsx/csv.
